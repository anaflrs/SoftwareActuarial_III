x <- 1 
x <- 2 
print(x)
#Mayúscula no definida no es lo mismo que la minúscula
#Impresión implicita:
x
#Asignación de textos:
msg <- "Hola" 
#Impresión de textos:
print(msg)
#x <- #Expresión incompleta, para salir ESC
# Impresión de un arreglo 
x <- 1:20
x
y <- 3:9
y
z <- 99:43
z
#El operador ":" me permite generar secuencias numéricas de un principio 
#a un fin 
w <- 3:14.5
w
#Si se trabaja con decimales, genera la secuencia hasta el entero correspondiente
Inf
1/0
exp(-Inf) #0
Inf + Inf #Inf
Inf * Inf #Inf
Inf - Inf #NaN
Inf/Inf #NaN

#Cada objeto tiene sus propiedades o atributos 

#Un vector se crea con la función c()
x <- c(TRUE,FALSE,TRUE,TRUE,F,F,T)  #logical 
x <- c()

